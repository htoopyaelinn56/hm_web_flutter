name: hm_web_flutter
description: "H&M Cosmetics"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.6.3
  cached_network_image: ^3.4.1
  intl: ^0.20.1
  flutter_riverpod: 2.6.1
  http: ^1.2.2
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - dotenv
